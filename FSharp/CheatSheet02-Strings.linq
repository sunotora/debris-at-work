<Query Kind="FSharpProgram" />

"2. Strings" |> stdout.WriteLine
let s = "This is a string"
let hello = "Hello" + "World"
let share = @"\\share"
let shareLn = "\\\\share\n"

s |> stdout.WriteLine
hello |> stdout.WriteLine
share |> stdout.WriteLine
shareLn |> stdout.WriteLine
